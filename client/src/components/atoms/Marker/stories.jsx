import Marker from "./index";

export default {
	title: "Atoms/marker",
	component: Marker,
};

const Template = (args) => <Marker {...args} />;

// export const Origninal = Template.bind({});
// Default.args = {
// }
